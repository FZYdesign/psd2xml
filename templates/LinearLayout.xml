<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android" 
	android:padding="10dp"
	android:gravity="center_vertical"
	{% for k, v in attr.iteritems() %}android:{{ k }}="{{ v }}"
	{% endfor %} >
	{{ childs }}
</LinearLayout>
