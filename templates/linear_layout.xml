<ImageView
	android:id="@+id/{{ info.name }}"
{% if info.width %}

	android:layout_width="{{ info.width }}"
{% else %}
	android:layout_width="wrap_content"
{% endif %}
	android:layout_height="wrap_content"
	android:src="@drawable/{{ info.name }}" />

