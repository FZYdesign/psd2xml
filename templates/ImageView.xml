{% for c in comment %}
<!-- {{ c }} -->{% endfor %}
<ImageView
	android:id="@+id/{{ info.name }}"
	android:layout_height="wrap_content"
	{% for k, v in info.iteritems() %}android:{{ k }}="{{ v }}"
	{% endfor %}/>

